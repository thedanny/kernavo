(function(){
        'use stricit';

            angular.module('cbo-scm').controller('vendorsController',controller);

            controller.$inject=['$scope','$location','AppSettings']

            function controller($scope,$location,AppSettings){

                var vm=this;


                function activate(){
                    

                }
               activate();
            
            }
})();
